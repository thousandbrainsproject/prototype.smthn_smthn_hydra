# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment/experiment_config: unsupervised_train
  - override /experiment/logging: training_silent
  - override /monty: evidence_1lm_surf_unsupervised_train
  - override /environment/env_interface_config: habitat_1sensor_surface
  - override /environment/train_env_interface_args: 10_distinct_predefined

experiment:
  config:
    max_total_steps: 4000
    n_eval_epochs: ${benchmarks.rotations_all_count}
