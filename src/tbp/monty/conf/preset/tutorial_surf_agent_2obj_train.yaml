# @package _global_
defaults:
  - /experiment/experiment_class: supervised_pretraining
  - /experiment/experiment_args: supervised_train_rotations
  - /experiment/logging: tutorial_silent
  - /monty: graph_1lm_surf_train
  - /environment/env_interface_config: habitat_1sensor_surface
  - /environment/train_env_interface_args: 2obj_mug_banana_predefined

experiment:
  config:
    monty_config:
      motor_system_config:
        motor_system_args:
          policy:
            action_sampler:
              rotation_degrees: 5.0
    logging:
      run_name: surf_agent_1lm_2obj_train
