# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment/experiment_config: eval
  - override /experiment/logging: eval_benchmark
  - override /monty: evidence_1lm_surf_ci
  - override /environment/env_interface_config: habitat_1sensor_surface
  - override /environment/eval_env_interface_args: 10_similar_predefined

experiment:
  config:
    n_eval_epochs: ${benchmarks.rotations_all_count}
