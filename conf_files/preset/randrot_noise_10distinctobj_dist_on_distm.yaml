# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment_config: eval
  - override /logging: eval_benchmark
  - override /monty_config: evidence_1lm_dist_noise
  - override /env_interface_config: habitat_1sensor_distance
  - override /eval_env_interface_args: 10_distinct_random

experiment:
  config:
    max_total_steps: 6000
    model_name_or_path: ${benchmarks.pretrained_dir}/supervised_pre_training_base/pretrained/
    monty_config:
      learning_module_configs:
        learning_module_0:
          learning_module_args:
            hypotheses_updater_args:
              max_nneighbors: 10
