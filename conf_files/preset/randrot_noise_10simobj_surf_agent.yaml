# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment_config: eval
  - override /logging: eval_benchmark
  - override /monty_config: evidence_1lm_surf_ci_noise
  - override /env_interface_config: habitat_1sensor_surface
  - override /eval_env_interface_args: 10_similar_random

experiment:
  config:
    max_total_steps: 6000
