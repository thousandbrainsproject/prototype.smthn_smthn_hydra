# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment_config: unsupervised_train
  - override /logging: training_silent
  - override /monty_config: evidence_1lm_surf_unsupervised_train
  - override /env_interface_config: habitat_1sensor_surface
  - override /train_env_interface_args: 10_similar_predefined

experiment:
  config:
    max_total_steps: 4000
    n_eval_epochs: ${benchmarks.rotations_all_count}
