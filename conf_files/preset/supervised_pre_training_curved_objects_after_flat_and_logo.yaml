# @package _global_
defaults:
  - override /experiment: supervised_pretraining
  - override /experiment_config: supervised_train_comp_lm0
  - override /logging: training_silent_info
  - override /monty_config: evidence_2lm_comp_monolithic_train
  - override /env_interface_config: habitat_2sensor_comp
  - override /train_env_interface_args: curved_objects_predefined

experiment:
  config:
    model_name_or_path: ${path.expanduser:${oc.env:MONTY_MODELS}/pretrained_ycb_v12/supervised_pre_training_logos_after_flat_objects/pretrained/}
