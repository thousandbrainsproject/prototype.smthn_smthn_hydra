# @package _global_
defaults:
  - override /experiment: object_recognition
  - override /experiment_config: eval_evidence
  - override /logging: eval_evidence
  - override /monty_config: evidence_1lm_worldimage
  - override /env_interface_config: saccade_on_image
  - override /eval_env_interface_args: world_image_standard

experiment:
  config:
    show_sensor_output: true
    logging:
      run_name: monty_meets_world_2dimage_inference
